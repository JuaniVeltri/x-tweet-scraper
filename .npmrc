# Supply-chain guard: refuse to install a version published less than a day ago,
# which is the window in which most npm account-takeover payloads are caught.
min-release-age=1
audit=false
fund=false
